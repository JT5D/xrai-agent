{
  "name": "xrai-agent",
  "version": "0.3.4",
  "description": "Zero-install recursive AI agent with evidence-gated skill evolution, browser repo execution, CLI, control room, and MCP.",
  "type": "module",
  "bin": {
    "xrai": "src/cli.js"
  },
  "scripts": {
    "start": "node src/cli.js",
    "chat": "node src/cli.js chat",
    "web": "node src/cli.js web",
    "mcp": "node src/cli.js mcp",
    "test": "node --test test/*.test.js",
    "check": "node --check src/*.js && node --check web/app.js && node --check web/local-agent.js && node --check web/browser-workspace.js && node --check web/input-guard.js && node --check web/static-runtime.js && node --check web/coi-bootstrap.js && node --check web/coi-sw.js && node --check web/state.js && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
