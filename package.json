{
  "name": "xrai-agent",
  "version": "0.3.0",
  "description": "Zero-install recursive AI agent with in-browser public-repo execution, evidence-gated skill evolution, CLI, control room, and MCP.",
  "type": "module",
  "bin": {
    "xrai": "src/cli.js"
  },
  "scripts": {
    "start": "node src/cli.js",
    "chat": "node src/cli.js chat",
    "web": "node src/cli.js web",
    "mcp": "node src/cli.js mcp",
    "test": "node --test test/*.test.js",
    "check": "node --check src/*.js && node --check web/app.js && node --check web/local-agent.js && node --check web/browser-workspace.js && node --check web/repo-runtime.js && node --check web/coi-bootstrap.js && node --check web/coi-sw.js && node --check web/state.js && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
